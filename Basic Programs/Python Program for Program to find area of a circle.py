# The area of a circle can simply be evaluated using the following formula.

# Area = pi * r2
# where r is radius of circle 

def area_of_cirle(r):
    pi = 3.14
    return pi * (r**2)

num = 4
print(area_of_cirle(4))